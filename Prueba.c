#include <stdio.h>

int main() {
    int num1, num2, num3, num4, num5, num6, num7, count;
    
    printf("Ingrese 7 números enteros separados por un espacio: ");
    scanf("%d %d %d %d %d %d %d", &num1, &num2, &num3, &num4, &num5, &num6, &num7);
    count = 0;
    if (num1 != 0) count++;
    if (num2 != 0) count++;
    if (num3 != 0) count++;
    if (num4 != 0) count++;
    if (num5 != 0) count++;
    
}
